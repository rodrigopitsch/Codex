();}
function groundY(){return Math.max(205,view.h-56)}
function makeEnemy(x0,type,i){const gy=groundY();if(type==='ghost')return{x:x0,y:gy-60-(i%2)*18,w:28,h:32,type,hp:2+Math.floor(caseIndex/2),maxHp:2+Math.floor(caseIndex/2),dir:i%2?1:-1,base:x0,speed:.55+.08*caseIndex,hit:0,on:true,phase:Math.random()*6};if(type==='hound')return{x:x0,y:gy-24,w:38,h:24,type,hp:3+Math.floor(caseIndex/2),maxHp:3+Math.floor(caseIndex/2),dir:i%2?1:-1,base:x0,speed:1+.1*caseIndex,hit:0,on:true,phase:0};return{x:x0,y:gy-35,w:26,h:35,type,hp:3+Math.floor(caseIndex/2),maxHp:3+Math.floor(caseIndex/2),dir:i%2?1:-1,base:x0,speed:.7+.08*caseIndex,hit:0,on:true,phase:0}}
function rects(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y}
function burst(x,y,color,n=7,s=3){for(let i=0;i<n;i++)particles.push({x,y,vx:(Math.random()-.5)*s,vy:(Math.random()-.8)*s,life:.35+Math.random()*.35,color,size:1+Math.random()*2})}
function toast(t){toastEl.textContent=t;toastEl.classList.add('on');toastTimer=1.6}
function objectiveMsg(t,d=3){objective.textContent=t;objective.classList.remove('hide');objectiveTimer=d}
function captureWorldState(){return JSON.parse(JSON.stringify({player:{...player,vx:0,vy:0,inv:0,shootCd:0},enemies,orbs,clues,boss,portal,checkpoint,gameTime}))}
function shiftSnapshotY(s,dy){if(!s||!dy)return;s.player.y+=dy;(s.enemies||[]).forEach(o=>o.y+=dy);(s.orbs||[]).forEach(o=>o.y+=dy);(s.clues||[]).forEach(o=>o.y+=dy);if(s.boss)s.boss.y+=dy;if(s.portal)s.portal.y+=dy;if(s.checkpoint)s.checkpoint.y+=dy}
function restoreWorldState(s){const z=JSON.parse(JSON.stringify(s));player=z.player;enemies=z.enemies;orbs=z.orbs;clues=z.clues;boss=z.boss;portal=z.portal;checkpoint=z.checkpoint;gameTime=z.gameTime;shots=[];particles=[];player.vx=0;player.vy=0;player.inv=1.25;keys.left=keys.right=keys.jump=keys.shoot=false;jumpPressed=shootPressed=false;bossBar.classList.toggle('on',!!(boss&&boss.on&&boss.awake));camera.x=Math.max(0,player.x-view.w*.30);camera.shake=0;updateHud()}
function respawn(){if(checkpointSnapshot)restoreWorldState(checkpointSnapshot);else buildWorld();toast(checkpoint&&checkpoint.lit?'Checkpoint: mundo restaurado':'Caso reiniciado')}
function damage(n,fromX){if(player.inv>0||state!=='play')return;player.hp-=n;player.inv=1;player.vx=fromX<player.x?3.6:-3.6;player.vy=-5.2;camera.shake=7;sfx('hit');burst(player.x+10,player.y+16,'#ff6674',9,4);updateHud();if(player.hp<=0){state='dead';setTimeout(()=>{if(state==='dead'){state='play';respawn()}},650)}}
function shoot(){if(player.shootCd>0)return;player.shootCd=.20;let vy=0,target=null,best=999;const pool=enemies.filter(e=>e.on);if(boss&&boss.on&&boss.awake)pool.push(boss);for(const e of pool){const dx=(e.x+e.w/2)-(player.x+player.w/2);if(Math.sign(dx)!==player.dir||Math.abs(dx)>235)continue;const dy=(e.y+e.h/2)-(player.y+15),score=Math.abs(dx)+Math.abs(dy)*1.8;if(Math.abs(dy)<95&&score<best){best=score;target=e}}if(target){const dx=(target.x+target.w/2)-(player.x+player.w/2),dy=(target.y+target.h/2)-(player.y+15);vy=Math.max(-2.6,Math.min(2.6,dy/Math.max(50,Math.abs(dx))*7.2))}shots.push({x:player.x+(player.dir>0?player.w:-9),y:player.y+13,w:11,h:7,vx:7.5*player.dir,vy,life:1,power:1+save.powerUp});burst(player.x+(player.dir>0?player.w:0),player.y+16,'#ffd36b',5,2.4);sfx('shoot')}
function physics(dt){const move=3.45+Math.min(.5,save.level*.03);if(keys.left){player.vx-=24*dt;player.dir=-1}else if(keys.right){player.vx+=24*dt;player.dir=1}else player.vx*=Math.pow(.0005,dt);player.vx=Math.max(-move,Math.min(move,player.vx));player.jumpBuffer=Math.max(0,player.jumpBuffer-dt);if(jumpPressed){player.jumpBuffer=.15;jumpPressed=false}if(player.onGround)player.coyote=.13;else player.coyote=Math.max(0,player.coyote-dt);if(player.jumpBuffer>0&&player.coyote>0){player.vy=-7.65;player.jumpBuffer=0;player.coyote=0;player.onGround=false;sfx('jump')}if(!keys.jump&&player.vy<0)player.vy+=17*dt;player.vy=Math.min(9,player.vy+18*dt);player.x+=player.vx*60*dt;player.x=Math.max(0,Math.min(CASES[caseIndex].length-player.w,player.x));const oldBottom=player.y+player.h;player.y+=player.vy*60*dt;player.onGround=false;for(const q of platforms){if(player.vy>=0&&player.x+player.w>q.x&&player.x<q.x+q.w&&oldBottom<=q.y+5&&player.y+player.h>=q.y){player.y=q.y-player.h;player.vy=0;player.onGround=true;break}}if(player.y>view.h+140){damage(99,player.x-1);return}if(shootPressed||keys.shoot){shoot();shootPressed=false}player.shootCd=Math.max(0,player.shootCd-dt);player.inv=Math.max(0,player.inv-dt);player.anim+=dt*(Math.abs(player.vx)>0.2?10:3)}
function updateWorld(dt){gameTime+=dt;physics(dt);for(const h of hazards)if(rects(player,h))damage(1,h.x);for(const s of shots){s.x+=s.vx*60*dt;s.y+=(s.vy||0)*60*dt;s.life-=dt}for(const e of enemies){if(!e.on)continue;e.hit=Math.max(0,e.hit-dt);if(e.type==='ghost'){e.x+=e.speed*e.dir*60*dt;e.y=groundY()-62+Math.sin(gameTime*2.7+e.phase)*13;if(Math.abs(e.x-e.base)>75)e.dir*=-1}else if(e.type==='hound'){if(Math.abs(player.x-e.x)<180)e.dir=player.x>e.x?1:-1;e.x+=e.speed*e.dir*60*dt;if(Math.abs(e.x-e.base)>130&&Math.abs(player.x-e.x)>180)e.dir*=-1}else{e.x+=e.speed*e.dir*60*dt;if(Math.abs(e.x-e.base)>85)e.dir*=-1}if(rects(player,e)){const stomp=player.vy>1.4&&player.y+player.h<e.y+e.h*.72;if(stomp){e.hp-=1+Math.floor(save.powerUp/2);player.vy=-6.1;camera.shake=3;burst(player.x+10,e.y,'#ffd36b',8,3);tone(210,.05,'square',.025);if(e.hp<=0){e.on=false;player.ess+=2+caseIndex;sfx('orb');updateHud()}}else damage(1,e.x)}for(const s of shots){if(s.life>0&&rects(s,e)){e.hp-=s.power;s.life=0;e.hit=.12;burst(s.x,s.y,'#c8a8ff',6,3);if(e.hp<=0){e.on=false;player.ess+=2+caseIndex;burst(e.x+e.w/2,e.y+e.h/2,'#72ddff',12,4);sfx('orb');updateHud()}}}}
 for(const o of orbs){if(!o.on)continue;o.t+=dt;const od=Math.hypot((player.x+10)-o.x,(player.y+16)-o.y);if(od<62&&od>1){o.x+=(player.x+10-o.x)*dt*4.5;o.y+=(player.y+16-o.y)*dt*4.5}if(od<24){o.on=false;player.ess++;sfx('orb');burst(o.x,o.y,'#72ddff',7,2);updateHud()}}
 for(const q of clues){if(!q.on)continue;if(